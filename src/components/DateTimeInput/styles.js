import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    input: {
        width: "90%",
        padding: 10,
        marginVertical: 10,
        marginHorizontal: 10,
        borderBottomWidth: 1,
        borderColor: "#4c168a"
    },

    inputIcon: {
        width: 22,
        height: 22,
        position: "absolute",
        top: 28,
        left: "85%",
        resizeMode: "contain"
    }
});

export default styles;